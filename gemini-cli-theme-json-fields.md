## 🧩 Gemini CLI Theme JSON Fields

| Field Name        | Purpose & Description |
|-------------------|------------------------|
| `name`            | The internal name of your theme (used to reference it in settings). |
| `type`            | Should be `"custom"` for user-defined themes. |
| `Background`      | The main background color of the interface. |
| `Foreground`      | The default text color — used for prompts, responses, and general UI text. |
| `LightBlue`       | Accent color used for lighter blue highlights or UI elements. |
| `AccentBlue`      | Primary blue accent — often used for links, buttons, or active elements. |
| `AccentPurple`    | Purple accent — used for syntax highlighting or UI decoration. |
| `AccentCyan`      | Cyan accent — often used for secondary highlights or subtle UI elements. |
| `AccentGreen`     | Green accent — used for success indicators or syntax elements. |
| `AccentYellow`    | Yellow accent — used for warnings, highlights, or syntax. |
| `AccentRed`       | Red accent — used for errors, deletions, or strong emphasis. |
| `Comment`         | Color used for comments, placeholder text, or muted UI text. |
| `Gray`            | Neutral gray — used for borders, inactive elements, or subtle dividers. |
| `DiffAdded`       | Color used to highlight added content (e.g., in diffs or comparisons). |
| `DiffRemoved`     | Color used to highlight removed content. |
| `DiffModified`    | Color used to highlight modified content. |
| `GradientColors`  | Array of colors used for gradient effects (e.g., loading bars, transitions). |
